# BikeMate

Мобильное приложение для велосипедистов: подбор и покупка запчастей, видео-гайды
по установке, покупка велосипеда, ИИ-помощник по ремонту и лента мест для катания.

## Стек

- **Expo (React Native) + TypeScript** — один код для iOS и Android.
- **expo-router** — файловая навигация, нижний tab bar.
- **Zustand** — локальное состояние (лента мест для катания).
- **Supabase** — хранилище фото для мест катания (опционально, есть локальный фолбэк).
- **Google Gemini API** (`gemini-2.5-flash` + Google Search grounding) — ИИ-чат по
  ремонту и веб-поиск реальных предложений на маркетплейсах/YouTube (есть демо-режим без ключа).
- **react-native-webview** — встроенный плеер YouTube для видео-гайдов.

Дизайн — в стиле iOS: системный шрифт, скруглённые карточки, мягкие тени,
поддержка светлой и тёмной темы (следует системной настройке).

## Что работает уже сейчас

С ключом `EXPO_PUBLIC_GEMINI_API_KEY`:
- Поиск запчастей по модели велосипеда — ИИ ищет в интернете (Google Search
  grounding через Gemini) реальные предложения на Ozon/Wildberries/Avito/AliExpress
  и подходящее видео на YouTube.
- Поиск, где купить велосипед — аналогично, через веб-поиск ИИ.
- ИИ-чат отвечает реальными советами по ремонту (может использовать веб-поиск
  для уточнения данных по конкретной модели).

Без ключа (демо-режим):
- Те же экраны показывают мок-данные/заготовленные ответы, чтобы интерфейс был
  полностью рабочим без затрат на API.

Независимо от ключа Gemini:
- Лента мест для катания с лайками, добавление своего места с фото
  (фото сохраняется локально на устройстве, пока не подключён Supabase).

## Какие API-ключи нужны для реальных данных

| Ключ | Для чего | Где получить |
|---|---|---|
| `EXPO_PUBLIC_GEMINI_API_KEY` | ИИ-чат по ремонту + веб-поиск реальных предложений на маркетплейсах и видео на YouTube | https://aistudio.google.com/app/apikey |
| `EXPO_PUBLIC_SUPABASE_URL` / `EXPO_PUBLIC_SUPABASE_ANON_KEY` | Облачное хранение фото мест катания | https://supabase.com/dashboard → создать проект → Project Settings → API |

Маркетплейсы (Ozon, Wildberries, Avito, AliExpress) не дают публичного API для
поиска цен сторонним приложениям, поэтому цены и ссылки на них находит сам ИИ
через веб-поиск (Gemini + Google Search grounding) — **это не официальный канал
данных, цены и наличие могут быть неточными или устаревшими**, в приложении это
явно помечено. Для надёжных данных в будущем — партнёрские/affiliate API
(например, у AliExpress есть Affiliate API, требует регистрации в программе).

> **Важно про безопасность:** переменные `EXPO_PUBLIC_*` попадают в собранное
> приложение и могут быть извлечены из APK/IPA. Для прототипа это нормально, но
> перед публикацией в стор перенесите вызовы Gemini на свой backend
> (например, Supabase Edge Function), который хранит ключ на сервере.
> Поскольку этот ключ публиковался в чате, после тестирования рекомендуется
> сгенерировать новый и удалить старый в https://aistudio.google.com/app/apikey.

## Установка и запуск с нуля

1. Установите Node.js 18+ и npm.
2. Установите зависимости:
   ```bash
   npm install --legacy-peer-deps
   ```
   (флаг `--legacy-peer-deps` нужен из-за конфликта пиров между React 19 и
   web-зависимостями `expo-router`; на работу приложения это не влияет.)
3. Скопируйте `.env.example` в `.env` и при желании заполните ключи:
   ```bash
   cp .env.example .env
   ```
4. Запустите проект:
   ```bash
   npx expo start
   ```
5. Откройте приложение:
   - На телефоне — установите **Expo Go** (App Store / Google Play) и
     отсканируйте QR-код из терминала.
   - В симуляторе iOS (нужен Mac + Xcode) — нажмите `i` в терминале.
   - В эмуляторе Android (нужен Android Studio) — нажмите `a` в терминале.

## Структура проекта

```
app/                    экраны (expo-router, файловая навигация)
  (tabs)/               три вкладки нижнего таб-бара
    index.tsx           Запчасти
    bikes.tsx           Велосипеды
    spots.tsx           Места для катания
  chat.tsx              экран ИИ-чата
  spot/[id].tsx         детали места
  spot/new.tsx          добавление нового места
src/
  components/           переиспользуемые UI-компоненты (Card, IOSButton, ...)
  data/mock.ts          мок-данные маркетплейсов, видео, мест катания (фолбэк без ключа)
  services/             gemini.ts, supabase.ts — интеграции с внешними API
  store/                Zustand-хранилища
  theme/                цвета, типографика, отступы (светлая/тёмная тема)
  types/                общие TypeScript-типы
```

## Дальнейшие шаги (после прототипа)

1. Подключить Supabase (Storage + Postgres) для постоянного хранения мест катания,
   лайков и комментариев между пользователями.
2. Перенести вызов Gemini API на backend-прокси.
3. Добавить карту (`react-native-maps`) на странице "Места для катания".
4. Изучить партнёрские API маркетплейсов для точных цен и наличия вместо веб-поиска ИИ.
